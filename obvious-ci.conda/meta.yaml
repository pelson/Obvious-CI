package:
    name: obvious-ci
    version: '0.3.0'

source:
    path: ../

requirements:
    build:
        - python
        - setuptools

    run:
        - python
        - setuptools
        - binstar
        - conda
        - conda-build
